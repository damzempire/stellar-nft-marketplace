{
  "name": "stellar-nft-marketplace",
  "version": "1.0.0",
  "description": "Complete NFT marketplace on Stellar using Soroban smart contracts",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend",
    "indexer"
  ],
  "scripts": {
    "install:all": "pnpm install",
    "build": "pnpm --filter contracts build && pnpm --filter frontend build && pnpm --filter indexer build",
    "dev": "pnpm --filter frontend dev",
    "dev:indexer": "pnpm --filter indexer dev",
    "test": "pnpm --filter contracts test && pnpm --filter frontend test && pnpm --filter indexer test",
    "clean": "pnpm --filter contracts clean && pnpm --filter frontend clean && pnpm --filter indexer clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
