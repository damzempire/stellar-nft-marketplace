{
  "name": "contracts",
  "version": "1.0.0",
  "scripts": {
    "build": "soroban contract build",
    "test": "cargo test",
    "clean": "cargo clean && rm -rf target"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^12.0.0"
  }
}
